name: virgool
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  persistent_bottom_nav_bar_v2: ^6.1.0  # اصلاح به نسخه درست
  flashy_tab_bar2: ^0.0.10
  convex_bottom_bar: ^3.2.0  # نگه داشتن برای ظاهر فانتزی
  curved_navigation_bar: ^1.0.6
  google_nav_bar: ^5.0.7
  salomon_bottom_bar: ^3.3.2
  animated_notch_bottom_bar: ^1.0.3
  fancy_bottom_navigation: ^0.3.3
  fluid_bottom_nav_bar: ^1.4.0
  circular_bottom_navigation: ^2.0.0
  dot_navigation_bar: ^1.0.2
  motion_tab_bar: ^2.0.4
  rolling_bottom_bar: ^0.2.2
  bottom_navy_bar: ^6.1.0
  titled_navigation_bar: ^5.0.0-nullsafety.0
  # fancy_bottom_navigation: ^0.3.3  # حذف به خاطر ناسازگاری
  # bubble_bottom_bar: ^2.0.0  # حذف به خاطر ناسازگاری و تداخل Badge

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/